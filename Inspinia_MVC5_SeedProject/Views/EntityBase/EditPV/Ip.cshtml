@model Inspinia_MVC5_SeedProject.Models.Ip
@using Inspinia_MVC5_SeedProject.App_LocalResources

<div class="modal-header">
    <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
    <h4 class="modal-title" id="myModalLabel">@Resource.EditHeaderIpAddr</h4>
</div>
<div class="modal-body">
    @using (Html.BeginForm("EditIp", "EntityBase"))
    {
        @Html.AntiForgeryToken()
        <div class="form-horizontal">
            @Html.ValidationSummary(true)
            <div class="form-group">
                @Html.LabelFor(model => model.ip, new { @class = "control-label col-md-2" })
                <div class="col-md-10">
                    @Html.EditorFor(model => model.ip)
                    @Html.ValidationMessageFor(model => model.ip)
                </div>
            </div>
            @Html.HiddenFor(model => model.id)
            @Html.HiddenFor(model => model.entityType)
        </div>
        <div class="modal-footer">
            <button type="button" class="btn btn-default" data-dismiss="modal">@Resource.CancelBtn</button>
            <button type="submit" class="btn btn-primary">@Resource.UpdateBtn</button>
        </div>
    }
</div>
